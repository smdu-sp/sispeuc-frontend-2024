import styled from "styled-components";

const Logo = styled.a`
  text-decoration: none;
  font-weight: 700;
  color: inherit;
  white-space: nowrap;
`;

export const HeaderLogo: React.FC = () => <Logo>SISPEUC</Logo>;